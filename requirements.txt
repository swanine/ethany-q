# Python 3.11+（亦可在 3.9+ 运行）
# macOS 自带/旧构建的 Python 常链接 LibreSSL 2.x，urllib3 2.x 会触发 NotOpenSSLWarning
# 并偶发与 requests 不兼容；锁定 1.26 LTS 可避免该问题。
urllib3>=1.26.18,<2
python-binance>=1.0.19
pandas>=2.0.0
numpy>=1.24.0
loguru>=0.7.2
python-dotenv>=1.0.0
websockets>=12.0
aiohttp>=3.9.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ── 机器学习依赖（可选，不安装时 ml_strategy 自动降级为 HOLD）──
# 基础 ML
scikit-learn>=1.3.0
joblib>=1.3.0

# LightGBM（推荐，速度快、内存占用低）
lightgbm>=4.0.0

# XGBoost（备选）
xgboost>=2.0.0

# LSTM（需要 PyTorch，仅在 ml_lstm 模式下使用）
# torch>=2.0.0  # macOS/Linux: pip install torch，根据 CUDA 版本选择

# ── Telegram 通知机器人 ──
python-telegram-bot[ext]>=20.0

# ── 可选：异步/ccxt 生态 ──
# ccxt>=4.2.0
